//Parameters a is the array 
//Reword use splice method to search for multiple instances in an array. if there are multiple instances in an array, take them out and then return the new array.
//Edge cases none
//Pseudo code

function distinct() {
  const array = []
  array.splice(i++){
    return []; 
  } 
}