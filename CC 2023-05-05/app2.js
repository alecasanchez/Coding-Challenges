function testEven(n) {
  return n % 2 == 0; 
}

// function checks to see if number passed is even. function returns true or false. number may be positive, negative INTEGERS or FLOATS. floats with non even decimals are considered un even. 
// pass a number function checks if true or false if even. integers /decomals are also checked and if passed return true. 

