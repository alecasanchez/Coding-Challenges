//Parameters are none
//REword find the derivarive of the parameters remember the calculatorfrom eloquent js

function derive(coefficient, exponent){
  return (coefficient * exponent) + "x^" + (exponent - 1)
};