// This is a long one. all of the parameters are arr. each instance musto complete a differnt task. So first of all I should read documentation and then practice the exercises and then teach them to my audience. Then I come back to codewars and try again.
const getLength = array => array.length;

const getFirst = array => array[0];

const getLast = array => array[array.length - 1];

const pushElement = array => array.concat(0);

const popElement = array => array.splice(0, array.length - 1);